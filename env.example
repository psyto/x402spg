# Target API Configuration
TARGET_API_URL=https://your-api-endpoint.com/api

# x402 Payment Configuration
# SPG_WALLET_KEYPAIR should be a JSON array of numbers representing your wallet's private key
# Example: [123,45,67,89,...]
SPG_WALLET_KEYPAIR=[Your wallet private key as JSON array]

# Facilitator address for x402 protocol
FACILITATOR_ADDRESS=your-facilitator-address-here

# Fee amount per API call (in SOL or USDC)
FEE_AMOUNT=0.001

# Solana cluster to use (devnet, mainnet-beta, or testnet)
SOLANA_CLUSTER=devnet

# Server Configuration (optional)
PORT=3000
PAYMENT_TIMEOUT_MS=30000

